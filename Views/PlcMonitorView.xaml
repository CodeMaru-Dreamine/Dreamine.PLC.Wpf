<UserControl x:Class="Dreamine.PLC.Wpf.Views.PlcMonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:Dreamine.PLC.Wpf.Converters"
             MinWidth="900"
             MinHeight="420">
    <UserControl.Resources>
        <conv:PlcConnectionStateBrushConverter x:Key="StateBrushConverter" />
    </UserControl.Resources>

    <Border Background="White"
            BorderBrush="#DDD"
            BorderThickness="1"
            CornerRadius="6"
            Padding="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" Margin="0,0,0,8">
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="PLC Monitor" FontSize="16" FontWeight="SemiBold" Margin="0,0,12,0" />
                    <Ellipse Width="10" Height="10" Fill="{Binding State, Converter={StaticResource StateBrushConverter}}" Margin="0,0,5,0" />
                    <TextBlock Text="{Binding State}" FontWeight="SemiBold" />
                    <TextBlock Text=" / " Foreground="#777" />
                    <TextBlock Text="{Binding ChannelName}" Foreground="#555" />
                </StackPanel>

                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Connect" Width="90" Margin="0,0,6,0" Command="{Binding ConnectCommand}" />
                    <Button Content="Disconnect" Width="90" Margin="0,0,6,0" Command="{Binding DisconnectCommand}" />
                    <Button Content="Clear Log" Width="90" Command="{Binding ClearLogCommand}" />
                </StackPanel>
            </DockPanel>

            <Grid Grid.Row="1" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="160" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="80" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Address" VerticalAlignment="Center" Margin="0,0,6,0" />
                <TextBox Grid.Column="1" Text="{Binding AddressText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,12,0" />
                <TextBlock Grid.Column="2" Text="Count" VerticalAlignment="Center" Margin="0,0,6,0" />
                <TextBox Grid.Column="3" Text="{Binding CountText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,12,0" />
                <TextBlock Grid.Column="4" Text="Address examples: D100, M10, X0, Y0, D100.0" Foreground="#777" VerticalAlignment="Center" />
            </Grid>

            <Grid Grid.Row="2" Margin="0,0,0,8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Bits" VerticalAlignment="Center" Margin="0,0,6,4" />
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding BitValuesText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,4" />
                <Button Grid.Row="0" Grid.Column="2" Content="Read Bits" Width="100" Margin="0,0,6,4" Command="{Binding ReadBitsCommand}" />
                <Button Grid.Row="0" Grid.Column="3" Content="Write Bits" Width="100" Margin="0,0,0,4" Command="{Binding WriteBitsCommand}" />

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Words" VerticalAlignment="Center" Margin="0,0,6,0" />
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding WordValuesText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0" />
                <Button Grid.Row="1" Grid.Column="2" Content="Read Words" Width="100" Margin="0,0,6,0" Command="{Binding ReadWordsCommand}" />
                <Button Grid.Row="1" Grid.Column="3" Content="Write Words" Width="100" Command="{Binding WriteWordsCommand}" />
            </Grid>

            <Grid Grid.Row="3">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="{Binding StatusMessage}" Foreground="#333" Margin="0,0,0,6" />

                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding Logs}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Time" Binding="{Binding Time, StringFormat=HH:mm:ss.fff}" Width="110" />
                        <DataGridTextColumn Header="Operation" Binding="{Binding Operation}" Width="110" />
                        <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="100" />
                        <DataGridTextColumn Header="Values" Binding="{Binding Values}" Width="220" />
                        <DataGridCheckBoxColumn Header="OK" Binding="{Binding IsSuccess}" Width="50" />
                        <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Grid>
    </Border>
</UserControl>
